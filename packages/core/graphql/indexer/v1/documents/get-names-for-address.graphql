query V1GetNamesForAddress($first: Int!, $skip: Int!, $where: Domain_filter!) {
  _meta {
    block {
      number
    }
  }
  domains(first: $first, skip: $skip, where: $where) {
    ...V1IndexedNameFields
  }
}
