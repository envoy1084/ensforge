query V2GetResolverMetadata($resolver: String!) {
  _meta {
    block {
      number
    }
  }
  metadata(resolver: $resolver) {
    id
    resolver
    graphqlUrl
    blockNumber
    timestamp
    transactionHash
  }
}
