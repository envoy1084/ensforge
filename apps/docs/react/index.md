---
title: React
description: Reactive ENS queries and mutations powered by Effect Atom.
---

# React

Reactive ENS queries and mutations powered by Effect Atom.

```tsx
const owner = useOwner({ name: "ens.eth" });
const setText = useSetText();
```

Start with [Why Ensforge](/react/why) or [Getting Started](/react/getting-started).
