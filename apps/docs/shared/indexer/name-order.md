`NameOrder | undefined`

Orders by `createdAt`, `name`, or `expiry` in `asc` or `desc` direction. The default is
`{ field: "createdAt", direction: "desc" }`. The same order must be retained when continuing from a
cursor.
