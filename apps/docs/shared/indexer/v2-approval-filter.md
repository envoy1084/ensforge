`ResolverApprovalFilter`

Narrows delegate approvals by `resolver`, `namehash`, `context`, `delegate`, approval state, block
range, or timestamp range. At least one bounded identity selector is required so the query cannot
scan the complete approval history.
