---
title: useIndexedName
description: React hook that returns the normalized indexed state for one ENS name.
---

import { ReadActionDemo } from "../../../../../../components/read-actions/read-action-demo.client.tsx";
import SharedSnippet0 from "../../../../../../shared/react/atom-parameters.mdx";
import SharedSnippet1 from "../../../../../../shared/react/atom-result.mdx";
import SharedSnippet2 from "../../../../../../shared/react/suspense-atom-parameters.mdx";
import SharedSnippet3 from "../../../../../../shared/react/suspense-atom-result.mdx";

# useIndexedName

React hook that returns the normalized indexed state for one ENS name.

## Import

```tsx
import { useIndexedName } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useIndexedName } from "@ensforge/react";

function Component() {
  const result = useIndexedName({ name: "example.eth" });

  return <pre>{JSON.stringify(result.data, null, 2)}</pre>;
}
```

```tsx [provider.tsx]
// [!include ~/snippets/react/provider.tsx]
```

:::

<ReadActionDemo action="indexer.getIndexedName" />

## Parameters

```ts
import type { UseEnsAtomParameters } from "@ensforge/react";
import type { GetIndexedNameParameters } from "@ensforge/sdk/indexer";
```

### name

`string | undefined`

ENS name to look up. Provide either `name` or `namehash`; names are normalized before querying.

### namehash

`` `0x${string}` | undefined ``

Namehash to use when the plaintext name is unavailable. Provide either `namehash` or `name`.

<SharedSnippet0 />

## Return Type

```ts
type Result = ReturnType<typeof useIndexedName>;
```

Successful `data` has type `GetIndexedNameResult`.

<SharedSnippet1 />

## Suspense

Use `useIndexedNameSuspense` beneath React [Suspense](https://react.dev/reference/react/Suspense) and an error boundary. It starts immediately, suspends during the initial load, and throws typed failures to the nearest error boundary.

### Import

```tsx
import { useIndexedNameSuspense } from "@ensforge/react";
```

### Usage

```tsx
const result = useIndexedNameSuspense({ name: "example.eth" });
```

### Parameters

`useIndexedNameSuspense` accepts the same operation parameters described above. Its Effect Atom options omit `enabled` because a Suspense read always executes.

<SharedSnippet2 />

### Return Type

```ts
type Result = ReturnType<typeof useIndexedNameSuspense>;
```

<SharedSnippet3 />

## Effect Atom

```ts
import { getIndexedNameAtom } from "@ensforge/react/atoms";
import { sdk } from "./client";

const atom = getIndexedNameAtom(sdk, parameters, options);
```

The hook creates this atom with the SDK and registry supplied by [`EnsforgeProvider`](/react/api/ensforge-provider).

## Actions

- [`getIndexedName`](/core/api/actions/indexer/names/get-indexed-name)
- [`sdk.indexer.getIndexedName`](/sdk/api/indexer/names/get-indexed-name)
