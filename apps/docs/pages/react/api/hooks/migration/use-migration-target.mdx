---
title: useMigrationTarget
description: Hook for fetching migration target.
---

import { ReadActionDemo } from "../../../../../components/read-actions/read-action-demo.client.tsx";
import SharedSnippet0 from "../../../../../shared/react/atom-parameters.mdx";
import SharedSnippet1 from "../../../../../shared/react/atom-result.mdx";
import SharedSnippet2 from "../../../../../shared/react/suspense-atom-parameters.mdx";
import SharedSnippet3 from "../../../../../shared/react/suspense-atom-result.mdx";

# useMigrationTarget

Hook for fetching migration target.

## Import

```tsx
import { useMigrationTarget } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useMigrationTarget } from "@ensforge/react";

function Component() {
  const result = useMigrationTarget({
    name: "example.eth",
  });

  return <pre>{JSON.stringify(result.data, null, 2)}</pre>;
}
```

```tsx [provider.tsx]
// [!include ~/snippets/react/provider.tsx]
```

:::

<ReadActionDemo action="migration.getMigrationTarget" />

## Parameters

```ts
import type { UseEnsAtomParameters } from "@ensforge/react";
import type { MigrationNameParameters } from "@ensforge/sdk/migration";
```

### name

`string`

ENS name used by the operation. ensforge normalizes it before creating the query key or interacting with a contract.

### blockNumber

`bigint | undefined`

Block number to read from. Cannot be combined with `blockTag`.

### blockTag

`"latest" | "earliest" | "pending" | "safe" | "finalized" | undefined`

Named block state to read from. Cannot be combined with `blockNumber`.

<SharedSnippet0 />

## Return Type

```ts
type Result = ReturnType<typeof useMigrationTarget>;
```

<SharedSnippet1 />

## Suspense

Use `useMigrationTargetSuspense` when the read belongs beneath React [Suspense](https://react.dev/reference/react/Suspense) and an error boundary. It starts immediately, suspends during the initial load, and throws typed failures to the nearest error boundary.

### Import

```tsx
import { useMigrationTargetSuspense } from "@ensforge/react";
```

### Usage

:::code-group

```tsx [component.tsx]
import { useMigrationTargetSuspense } from "@ensforge/react";

function Component() {
  const result = useMigrationTargetSuspense({
    name: "example.eth",
  });

  return <pre>{JSON.stringify(result.data, null, 2)}</pre>;
}
```

```tsx [provider.tsx]
// [!include ~/snippets/react/provider.tsx]
```

:::

### Parameters

`useMigrationTargetSuspense` accepts the same operation parameters described above. Its Effect Atom options omit `enabled` because a Suspense read always executes.

```ts
import type { UseEnsSuspenseAtomParameters } from "@ensforge/react";
import type { MigrationNameParameters } from "@ensforge/sdk/migration";
```

<SharedSnippet2 />

### Return Type

```ts
type Result = ReturnType<typeof useMigrationTargetSuspense>;
```

<SharedSnippet3 />

## Effect Atom

```ts
import { getMigrationTargetAtom } from "@ensforge/react/atoms";
import { sdk } from "./client";

const atom = getMigrationTargetAtom(sdk, parameters, options);
```

The hook creates this atom with the SDK and registry supplied by [`EnsforgeProvider`](/react/api/ensforge-provider).

## Action

- [`getMigrationTarget`](/core/api/actions/migration/get-migration-target)
- [`sdk.migration.getMigrationTarget`](/sdk/api/migration/get-migration-target)
