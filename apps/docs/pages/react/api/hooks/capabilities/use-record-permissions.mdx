---
title: useRecordPermissions
description: Hook for fetching record permissions.
---

import SharedSnippet0 from "../../../../../shared/react/atom-parameters.mdx";
import SharedSnippet1 from "../../../../../shared/react/atom-result.mdx";
import SharedSnippet2 from "../../../../../shared/react/suspense-atom-parameters.mdx";
import SharedSnippet3 from "../../../../../shared/react/suspense-atom-result.mdx";

# useRecordPermissions

Hook for fetching record permissions.

## Import

```tsx
import { useRecordPermissions } from "@ensforge/react";
```

## Usage

:::code-group

```tsx [component.tsx]
import { useRecordPermissions } from "@ensforge/react";

function Component() {
  const result = useRecordPermissions({
    name: "example.eth",
    account: {},
    records: [],
  });

  return <pre>{JSON.stringify(result.data, null, 2)}</pre>;
}
```

```tsx [provider.tsx]
// [!include ~/snippets/react/provider.tsx]
```

:::

## Parameters

```ts
import type { UseEnsAtomParameters } from "@ensforge/react";
import type { GetRecordPermissionsParameters } from "@ensforge/sdk/capabilities";
```

### name

`string`

ENS name used by the operation. ensforge normalizes it before creating the query key or interacting with a contract.

### blockNumber

`bigint | undefined`

Block number to read from. Cannot be combined with `blockTag`.

### blockTag

`"latest" | "earliest" | "pending" | "safe" | "finalized" | undefined`

Named block state to read from. Cannot be combined with `blockNumber`.

### account

`EthereumAddress`

Account used to authorize the mutation. Defaults to the active wallet account.

### records

`ReadonlyArray<RecordOperation>`

Records selected, read, or written.

<SharedSnippet0 />

## Return Type

```ts
type Result = ReturnType<typeof useRecordPermissions>;
```

<SharedSnippet1 />

## Suspense

Use `useRecordPermissionsSuspense` when the read belongs beneath React [Suspense](https://react.dev/reference/react/Suspense) and an error boundary. It starts immediately, suspends during the initial load, and throws typed failures to the nearest error boundary.

### Import

```tsx
import { useRecordPermissionsSuspense } from "@ensforge/react";
```

### Usage

:::code-group

```tsx [component.tsx]
import { useRecordPermissionsSuspense } from "@ensforge/react";

function Component() {
  const result = useRecordPermissionsSuspense({
    name: "example.eth",
    account: {},
    records: [],
  });

  return <pre>{JSON.stringify(result.data, null, 2)}</pre>;
}
```

```tsx [provider.tsx]
// [!include ~/snippets/react/provider.tsx]
```

:::

### Parameters

`useRecordPermissionsSuspense` accepts the same operation parameters described above. Its Effect Atom options omit `enabled` because a Suspense read always executes.

```ts
import type { UseEnsSuspenseAtomParameters } from "@ensforge/react";
import type { GetRecordPermissionsParameters } from "@ensforge/sdk/capabilities";
```

<SharedSnippet2 />

### Return Type

```ts
type Result = ReturnType<typeof useRecordPermissionsSuspense>;
```

<SharedSnippet3 />

## Effect Atom

```ts
import { getRecordPermissionsAtom } from "@ensforge/react/atoms";
import { sdk } from "./client";

const atom = getRecordPermissionsAtom(sdk, parameters, options);
```

The hook creates this atom with the SDK and registry supplied by [`EnsforgeProvider`](/react/api/ensforge-provider).

## Action

- [`getRecordPermissions`](/core/api/actions/capabilities/get-record-permissions)
- [`sdk.capabilities.getRecordPermissions`](/sdk/api/capabilities/get-record-permissions)
