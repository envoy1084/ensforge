---
title: HCA Owner And Session Validator
description: Experimental ENSv2 hybrid contract account definition.
---

import SharedSnippet0 from "../../../../../../shared/contracts/entrypoint.mdx";
import SharedSnippet1 from "../../../../../../shared/contracts/type-safety.mdx";

# HCA Owner And Session Validator

Experimental ENSv2 hybrid contract account definition.

:::warning Experimental
This module is not part of the stable ENSv2 surface. Pin the package version and review release notes before upgrading.
:::

## Import

```ts
import { hcaOwnerAndSessionValidatorV2Abi } from "@ensforge/contracts/v2/experimental/hca";
```

## Usage

:::code-group

```ts [contract.ts]
import { getContract } from "viem";

const contract = getContract({
  address,
  abi: hcaOwnerAndSessionValidatorV2Abi,
  client: publicClient,
});
```

```ts [client.ts]
// [!include ~/snippets/contracts/client.ts]
```

:::

## Exports

| Export                             | Description                                             |
| ---------------------------------- | ------------------------------------------------------- |
| `hcaOwnerAndSessionValidatorV2Abi` | Immutable ABI value with viem-compatible literal types. |

## Entrypoint

`@ensforge/contracts/v2/experimental/hca`

<SharedSnippet0 />

## When to use

This export belongs to the experimental ENSv2 surface. Pin the package version and verify the selected deployment before using it in production.

## Type Safety

<SharedSnippet1 />

```ts
type Export = typeof hcaOwnerAndSessionValidatorV2Abi;
```
